# WebSocket Server Configuration
# Copy this file to .env.local and adjust values as needed

# Port for the standalone WebSocket server (server-side only)
WS_PORT=3001

# Client-side accessible WebSocket port (must have NEXT_PUBLIC_ prefix)
# This should match WS_PORT for the client to connect correctly
client=3001
